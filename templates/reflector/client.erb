protocol bgp '<%= @name %>' {
  description "Connection to node <%= @name %>";
  local as <%= @local_as %>;
  neighbor <%= @peer_ip %> as <%= @peer_as %>;
  multihop;
  rr client;
  graceful restart;
  import all;
  export all;
}
