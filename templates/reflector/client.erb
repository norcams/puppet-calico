protocol bgp '<%= @name %>' {
  description "Connection to node <%= @name %>";
  local as <%= @local_as %>;
  neighbor <%= @client_ip %> as <%= @client_as %>;
  multihop;
  rr client;
  graceful restart;
  import all;
  export all;
}
